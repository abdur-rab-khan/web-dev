/* 
STEP 1:

=> (SAME-DOCUMENT): To Trigger a ViewTransition the DOM change should be in "document.startViewTransition()" callback function.

=> (FOR CROSS DOCUMENT): use "@view-transition" at rule, "https://developer.mozilla.org/en-US/docs/Web/CSS/@view-transition"
 

STEP 2:


*/
