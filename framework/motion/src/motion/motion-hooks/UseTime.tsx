// It is used to give motion value that updates once per frame in millisecond, It's particularly good where continuous animation is needed.
// const time = useTime();
//const rotate = useTransform(time, [0, 4000], [0, 360], { clamp: false }); // "clamp: false" means it won't stop after input range exceeds
